const cards=[...document.querySelectorAll('.card')],filters=[...document.querySelectorAll('.filter')],search=document.querySelector('#search'),empty=document.querySelector('#empty'),toast=document.querySelector('#toast');let active='all';document.querySelector('#count').textContent=cards.length;function render(){const q=search.value.trim().toLowerCase();let shown=0;cards.forEach(c=>{const ok=(active==='all'||c.dataset.category===active)&&(!q||(c.dataset.search||'').includes(q));c.hidden=!ok;if(ok)shown++});empty.hidden=shown!==0}filters.forEach(b=>b.addEventListener('click',()=>{active=b.dataset.filter;filters.forEach(x=>x.classList.toggle('active',x===b));render()}));search.addEventListener('input',render);function copied(){toast.classList.add('show');clearTimeout(copied.t);copied.t=setTimeout(()=>toast.classList.remove('show'),1800)}document.querySelectorAll('.copy').forEach(b=>b.addEventListener('click',async()=>{const text=b.dataset.copy;try{await navigator.clipboard.writeText(text)}catch{const t=document.createElement('textarea');t.value=text;t.style.position='fixed';t.style.opacity='0';document.body.appendChild(t);t.select();document.execCommand('copy');t.remove()}const old=b.textContent;b.textContent='Copied';b.classList.add('copied');copied();clearTimeout(b.t);b.t=setTimeout(()=>{b.textContent=old;b.classList.remove('copied')},1500)}));render();
