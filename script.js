const cards=[...document.querySelectorAll(".card")],filters=[...document.querySelectorAll(".filters button")],search=document.getElementById("search"),empty=document.getElementById("empty"),count=document.getElementById("count"),toast=document.getElementById("toast");let active="all";function render(){const q=search.value.trim().toLowerCase();let n=0;cards.forEach(c=>{const ok=(active==="all"||c.dataset.category===active)&&(!q||c.dataset.search.includes(q)||c.innerText.toLowerCase().includes(q));c.hidden=!ok;if(ok)n++});count.textContent=`${n} snippet${n===1?"":"s"}`;empty.hidden=n!==0}filters.forEach(b=>b.onclick=()=>{filters.forEach(x=>x.classList.remove("active"));b.classList.add("active");active=b.dataset.filter;render()});search.oninput=render;document.querySelectorAll(".copy").forEach(b=>b.onclick=async()=>{const text=b.dataset.copy;try{await navigator.clipboard.writeText(text)}catch{const t=document.createElement("textarea");t.value=text;document.body.appendChild(t);t.select();document.execCommand("copy");t.remove()}const old=b.textContent;b.textContent="copied";toast.classList.add("show");setTimeout(()=>{b.textContent=old;toast.classList.remove("show")},1300)});render();