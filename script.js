const items=[...document.querySelectorAll(".item")],buttons=[...document.querySelectorAll(".filters button")],search=document.getElementById("search"),empty=document.getElementById("empty"),toast=document.getElementById("toast");let filter="all";function render(){const q=search.value.toLowerCase().trim();let n=0;items.forEach(x=>{const ok=(filter==="all"||x.dataset.category===filter)&&(!q||x.dataset.search.includes(q)||x.innerText.toLowerCase().includes(q));x.hidden=!ok;if(ok)n++});empty.hidden=n>0}buttons.forEach(b=>b.onclick=()=>{buttons.forEach(x=>x.classList.remove("active"));b.classList.add("active");filter=b.dataset.filter;render()});search.oninput=render;document.querySelectorAll(".copy").forEach(b=>b.onclick=async()=>{const text=b.dataset.copy;try{await navigator.clipboard.writeText(text)}catch{const t=document.createElement("textarea");t.value=text;document.body.appendChild(t);t.select();document.execCommand("copy");t.remove()}toast.textContent="Import copied";toast.classList.add("show");setTimeout(()=>toast.classList.remove("show"),1200)});render();